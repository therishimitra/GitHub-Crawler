"GitHub v3.py" improves on the shortcomings of the previous versions. By filtering out the required
fields in "real-time" during the crawling of data instead of extracting it later from an intermediate 
CSV file the, a more complete final database is obtained in the form of "GitHub_numpy_database.csv".
(However, the actual reason why "GitHub v2.py" failed to achieve this is still uncertain)  